// Usando o for.

#include <stdio.h>

int main(void) {
    int contador;

    for (contador = 1; contador <= 10; contador++) { // Corrigido "contador" para "contador"
        printf("%d\n", contador); // Adicionando quebra de linha
    }

    return 0; // Adicionando instrução de retorno
}
